// Este é um arquivo de exemplo para compressão
(function() {
  const saudacao = "Olá, Grunt!";
  console.log(saudacao);
  
  function mostrarAlerta() {
    alert("Código JavaScript comprimido!");
  }

  // Mostra um alerta após 3 segundos
  setTimeout(mostrarAlerta, 3000);
})();
